# Spanish Dub Study v2.30.0 runtime stability

Device trace-driven fixes after v2.29:

- Keep the working character-aware OpenRouter token budget; do not raise it further.
- Restore unique numbered-slot tracking and contiguous-prefix recovery so noncontiguous/duplicate model line numbers cannot be mistaken for an aligned prefix.
- Withhold source-English leaks during streaming and repair guarded final slots with singleton Google fallback.
- Join an already-running identical progressive Edge prefetch instead of synthesizing the same segment twice.
- Prevent subtitle pagination from moving backward when actual Edge MediaPlayer timing hands control back to the video clock.
- Latch deterministic Android Visualizer error -3 for the process so speaker probing stops retrying an unsupported capture path.
- Suppress diagnostic noise from test-voice cache entries (segment index -1).
- Update the diagnostic header to the actual release version and architecture.
